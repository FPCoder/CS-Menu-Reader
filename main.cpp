/* To Do: 
	Read list files
	Create Vector for Item names
	Check prices on each list file and find average
	put averages into separate vector
	print vectors*/

#include <iostream>
#include <vector>
#include <fstream>
using namespace std;

int main(int argc, char *argv[]) {
	int size = argc;
	//Creates a vector containing all item names
	for (int i = 1; i < size; i++) {

	}


    return 0;
}
